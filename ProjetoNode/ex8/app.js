const readline = require("readline");
const teclado = readline.createInterface({
    input: process.stdin,
    output: process.stdout
});
now = new Date
teclado.question(
    
            console.log(  "Hoje é "  + now.getDate() + " do " + now.getMonth() + " de " + now.getFullYear())
            
      
);